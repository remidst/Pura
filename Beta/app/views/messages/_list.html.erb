<table class="table table-striped">
	<tbody>
	<% @messages.each do |msg| %>
		<tr>
			<td><%= msg.created_at %></td>
			<td><%= msg.user_id %></td>
			<td><%= msg.content %></td>
		</tr>
	<% end %>
	</tbody>
</table>