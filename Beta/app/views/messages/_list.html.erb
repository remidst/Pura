<% @messages.each do |msg| %>
	<div class="message-block">
		<div class="message-info">
			<%= msg.user.try(:username) %>
		<%= msg.created_at.try(:strftime, '%Y-%m-%d') %>
		</div>
		<div class="message-content">
			<%= msg.content %>
		</div>
	</div>
<% end %>
