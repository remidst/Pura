<% @conversations.each do |conversation| %>
	<div class="conversation-messages" id="messages-<%= conversation.id %>">
		<% conversation.messages.each do |msg| %>
			<div class="messages">
				<div class="message-info">
					<%= msg.user.try(:username) %>
				    <%= msg.created_at.try(:strftime, '%Y-%m-%d') %>
				</div>
				<div class="message-content">
					<%= msg.content %>
				</div>
			</div>
		<% end %>
	</div>
<% end %>




