<%= form_for([@project, @project.messages.build]) do |f| %>
  <p class="form_group">
    <%= f.label :content, 'Add new message:' %><br>
    <%= f.text_area :content, class: 'form-control' %>
  </p>
  <p class="form_group">
    <%= f.submit 'Send', class: 'btn btn-info' %>
  </p>
<% end %>