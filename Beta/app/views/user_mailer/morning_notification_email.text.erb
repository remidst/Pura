<%= @user.username %>様、

新規メッセージが共有されました。

メッセージ：
<% @notifications.each do |notification| %>
		<%= notification.content %>案件：<%= notification.project.project_name %>様。<%= link_to '案件へ', project_url(notification.project, user_email: @user.email, user_token: @user.authentication_token) %>
<% end %>