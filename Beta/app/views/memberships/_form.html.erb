	<%= form_for([@project, @project.memberships.build]) do |f| %>
	  <p class="form_group">
	  	<%= f.label :user_id, 'メンバーを追加する' %>
	    <%= f.collection_select(:user_id, @potential_users, :id, :email, :prompt => true) %>
	  </p>
	  <p class="form_group">
	    <%= f.submit '追加する', class: 'btn btn-info' %>
	  </p>
	<% end %>

	