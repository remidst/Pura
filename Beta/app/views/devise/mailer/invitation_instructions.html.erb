<p><%= @resource.email %>様</p>

<p><%= @resource.invited_by.company %>の<%= @resource.invited_by.username %>様からプラプラで作成された案件に招待されました。</p>

<p>プラプラは安全性と使いやすさを重視したクラウド型のプラットフォームを提供し、毎日の仕事場でのやり取りをより円滑にします。</p>

<%= link_to "登録する", accept_invitation_url(@resource, invitation_token: @token) %>

 <% if @resource.invitation_due_at %>
  <p><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<% end %>
