<%= @leader.username %>様

'<%= @project.project_name %>'の案件から次のメンバーが削除されました：

<% @deleted.each do |user| %>
	<% if user.username.present? %>
		<%= user.username %>様, <%= user.company %>
	<% else %>
	<% end %>
<% end %>

<%= link_to '案件を見る', project_url(@project, user_email: @user.email, user_token: @user.authentication_token) %>