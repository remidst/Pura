<p>
	<%= @user.username %>様、<%= @project.project_name %>'の案件が共有されました。案件内に他のメンバーにファイルやメッセージが共有できます。
</p>

<div>
	案件に招待されたメンバー：
		<% @users.each do |usr| %>
			<div>
				<%= usr.username %> <%= usr.company %>
			</div>
		<% end %>
</div>
