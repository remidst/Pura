<%= @user.username %>様、<%= @leader.username %>様から<%= @project.project_name %>様の案件に招待されました。

Purapuraで出来ること
1) 担当者間でメッセージのやり取り
2) 担当者間でレポート、提供票など、ファイルの共有


<%= link_to '案件を見る', project_url(@project) %>


案件のメンバー：
<% @users.each do |user| %>
<%= user.username %>様, <%= user.company %>
<% end %>
