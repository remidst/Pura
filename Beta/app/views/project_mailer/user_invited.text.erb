<%= @user.username %>様、<%= @leader.username %>から'<%= @project.project_name %>'の案件に招待されました。

他のメンバーとメッセージとファイルの共有をしながら、案件に関する仕事がより円滑にできます。


<%= project_url(@project) %>


案件のメンバー：
<% @project.users.each do |user| %>
<%= user.username %>, <%= user.company %>
<% end %>
