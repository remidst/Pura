<table class="table table-striped">
  <thead>
  	<tr>
  		<th>ファイル名</th>
      <th>ユーザー</th>
  		<th>ダウンロード</th>
  		<th>消す</th>
  	</tr>
  </thead>
  <tbody>
  	<% if @documents.each do |document| %>
		<tr>
			<td><%= document.attachment %></td>
      <td><%= document.user.try(:email) %></td>
			<td><%= link_to "ダウンロード", document.attachment_url %></td>
			<td><%= button_to "消す",  [document.project, document], method: :delete, class: "btn btn-danger", confirm: "このファイルは削除されます" %></td>
		</tr> 
	<% end.empty? %>
  <% end %>
  </tbody>
</table>
   
