<%= form_with(model: project, local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2> エラーが<%= pluralize(project.errors.count, "error") %>件発生しました:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form_group">
    <%= form.label :project_name, '利用者名:' %>
    <%= form.text_field :project_name, id: :project_name, :required => 'required', class: "form-control" %>
  </div>

  <div class="form_group">
    <%= form.label :leader_id, '案件の担当マネジャー:' %>
    <%= form.collection_select(:leader_id, @registered, :id, :username, :prompt => true, selected: @project.leader_id.to_i) %>
  </div>

  <div class="actions">
    <%= form.submit '登録する', class: "btn btn-info" %>
  </div>
<% end %>
