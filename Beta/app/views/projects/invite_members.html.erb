<div class="container-fluid">
	<div class="well membership-index-well">
		<h1>'<%= @project.project_name %>'の案件にメンバーを招待する</h1>

		<%= render 'invite_members' %>

		<div class="membership-form">
			<%= render 'memberships/invite_members' %>
		</div>
	

		<%= link_to 'メンバーを招待せずに進む', project_path(@project) %>
	</div>
	
</div>